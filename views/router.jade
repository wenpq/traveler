extends layout

block head
  meta(charset='urf-8')
  title FRIB Traveler :: router :: #{_id}
  meta(name='viewport', content='width=device-width, initial-scale=1.0')
  link(rel='stylesheet', href='/bootstrap/css/bootstrap.css')
  link(rel='stylesheet', href='/bootstrap/css/bootstrap-responsive.css')
  link(rel='stylesheet', href='/font-awesome-4.0.3/css/font-awesome.css')
  link(rel='stylesheet', href='/stylesheets/style.css')

block content
  .container
    #message
    #modal.modal.hide.fade(tabindex='-1',role='dialog',data-backdrop='static',aria-labelledby='modalLabel',aria-hidden='true')
      .modal-header
        h3#modalLabel
      .modal-body
      .modal-footer
    h3 Router title:  #{title}
    if (locals.status !== undefined)
      h4 Status:
        case locals.status
          when 0
            = ' '
            span initialized
          when 1
            = ' '
            span active
          when 2
            = ' '
            span completed
          when 3
            = ' '
            span aborted
          default
            = ' '
    if (locals.status !== undefined && locals.status == 1)
      .btn-group
        button.btn.btn-primary#complete Submit for completion
    .btn-group
      button.btn.btn-info(data-toggle='collapse', data-target='#details') Details
    #details.collapse.out
      if (locals.description !== undefined)
        h4 Description: #{description}
      p Created by #{createdBy}
        = ' '
        if (locals.clonedBy !== undefined)
          | cloned by #{clonedBy} on
        else
          | on
        = ' '
        span.time #{createdOn}
      if (locals.updatedBy !== undefined)
        p Updated by #{updatedBy} on
          = ' '
          span.time #{updatedOn}
    br

block js
  script(type='text/javascript', src='/jquery/jquery-1.9.1.js')
  script(type='text/javascript', src='/bootstrap/js/bootstrap.js')
  // script(type='text/javascript', src='/dependencies/modernizr.js')
  // script(type='text/javascript', src='/dependencies/binder.js')
  script(type='text/javascript', src='/dependencies/moment.js')
  script(type='text/javascript', src='/dependencies/livestamp.js')
  // script(type='text/javascript', src='/bootstrap-datepicker/js/bootstrap-datepicker.js')
  // script(type='text/javascript', src='/javascripts/router.js')
